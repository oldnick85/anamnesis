message("PROC LANGUAGE PARAMETERS")
if(NOT DEFAULT_LANGUAGE)
    set(DEFAULT_LANGUAGE RU)
endif(NOT DEFAULT_LANGUAGE)

set(DOCUMENTATION_LANGUAGE Russian)
if((DEFAULT_LANGUAGE STREQUAL RU) OR (DEFAULT_LANGUAGE STREQUAL ru))
    set(DOCUMENTATION_LANGUAGE Russian)
endif((DEFAULT_LANGUAGE STREQUAL RU) OR (DEFAULT_LANGUAGE STREQUAL ru))
if((DEFAULT_LANGUAGE STREQUAL EN) OR (DEFAULT_LANGUAGE STREQUAL en))
    set(DOCUMENTATION_LANGUAGE English)
endif((DEFAULT_LANGUAGE STREQUAL EN) OR (DEFAULT_LANGUAGE STREQUAL en))
message("DEFAULT_LANGUAGE=" ${DEFAULT_LANGUAGE})
message("DOCUMENTATION_LANGUAGE=" ${DOCUMENTATION_LANGUAGE})
